{
  "name": "travel-diary-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for travel diary app",
  "main": "index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "itty-router": "^4.0.0",
    "jwt": "^0.2.0",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}